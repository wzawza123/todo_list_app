# MD Task Manager — 产品功能开发文档 (PRD)

> 版本 v1.0 · 2026-07-21
> 本文档面向 coding agent，作为完整的开发指导。除标注为"开放决策"的条目外，所有设计均为已定案，agent 不应自行更改数据格式与核心交互设计。

---

## 1. 背景与问题

用户目前使用 Obsidian 进行项目管理，任务以 `- [ ]` checkbox 形式散落在多个 markdown 文件中。存在以下痛点：

1. **无法聚合**：分散在各个 md 文件中的 todo 无法统一查看，缺乏全局视图。
2. **依赖关系无法表达**：任务之间存在前置依赖，但 markdown 原生语法无法表示，导致排序混乱。
3. **无法清晰排序**：缺乏优先级机制与手动排序能力。
4. **没有"今天做什么"的答案**：缺少一个每日执行清单（Today view），无法从任务库中快速圈选当日任务并安排执行顺序。

## 2. 产品目标

构建一个**本地运行的单用户 web app**，作为 markdown 任务库之上的管理界面：

- 持久化**完全基于 md 文件**——md 文件是唯一事实来源（single source of truth），app 不引入数据库。用户随时可以脱离本 app，直接在 Obsidian / 任意编辑器中查看和编辑任务。
- 提供聚合视图、依赖表达、优先级快速标注、Inbox 快速收集、今日日程编排能力。

### 非目标（明确不做）

- 多用户 / 协作 / 权限。
- 云同步、账号系统。
- 日历时间块（time blocking）、番茄钟、提醒推送。
- 富文本编辑器（任务描述保持纯 markdown 文本）。
- 移动端适配（桌面浏览器优先，宽度 ≥ 1280px）。

---

## 3. 核心概念与数据模型

### 3.1 Task（任务）

| 字段 | 类型 | 说明 | 持久化位置 |
|---|---|---|---|
| `id` | string | 6 位 base36 随机 ID（如 `k3x9ab`），全库唯一 | 行内 `🆔 k3x9ab` |
| `title` | string | 任务标题（单行 markdown 文本） | checkbox 行正文 |
| `status` | enum | `todo` / `done` | `- [ ]` / `- [x]` |
| `priority` | enum | `highest` / `high` / `medium` / `low` / `none` | `🔺` / `⏫` / `🔼` / `🔽` / 无标记 |
| `depends_on` | string[] | 前置任务 id 列表 | 行内 `⛔ id1,id2` |
| `due` | date? | 截止日期（可选，非 MVP 核心） | 行内 `📅 2026-07-25` |
| `done_date` | date? | 完成日期 | 行内 `✅ 2026-07-21` |
| `children` | Task[] | 子任务，**最多嵌套 4 级**（根任务为第 1 级） | 缩进的子 checkbox 行 |
| `file` | string | 所属 md 文件相对路径 | 由所在文件决定 |
| `line` | int | 在文件中的行号（运行时字段，不持久化） | — |

> **语法兼容性要求**：行内元数据采用 **Obsidian Tasks 插件的 emoji 语法**（`🆔`、`⛔`、`🔺⏫🔼🔽`、`📅`、`✅`）。这保证用户在 Obsidian 中安装 Tasks 插件后，本 app 写入的数据可被 Obsidian 侧原样识别，反之亦然。解析与序列化必须严格遵循该语法，元数据统一追加在标题文本之后。

**示例任务行：**

```markdown
- [ ] 完成 SR 模型的 ablation 实验 ⏫ 🆔 k3x9ab 📅 2026-07-25
    - [ ] 准备 baseline checkpoint 🆔 m2p4qq
    - [ ] 跑 4 组消融配置 🆔 n8r2ws ⛔ m2p4qq
- [x] 写实验设计文档 🆔 a1b2c3 ✅ 2026-07-20
```

### 3.2 任务库（Vault）

- App 启动时通过配置指定一个**根目录**（可以就是用户的 Obsidian vault，或其中的子目录）。
- 递归扫描根目录下所有 `.md` 文件（排除 `.obsidian/`、`.trash/`、隐藏目录），解析其中所有 checkbox 行为任务。
- 不含 checkbox 的 md 文件忽略；同一文件内的普通文本内容不做任何改动——**写回时必须保持非任务行逐字节不变**。

### 3.3 Inbox

- 固定文件：根目录下的 `Inbox.md`（不存在则自动创建）。
- 快速添加的任务全部追加到该文件末尾，后续由用户手动移动/整理（MVP 中"移动到其他文件"可以通过在 Obsidian 中剪切粘贴完成，app 内移动为 M2 功能）。

### 3.4 今日日程（Today）

- 核心设计：**引用而非复制**。今日日程是一个有序的任务 id 列表，不复制任务文本，避免双写不一致。
- 持久化文件：`Today/YYYY-MM-DD.md`，格式如下：

```markdown
---
date: 2026-07-21
---

- [[k3x9ab]]
- [[n8r2ws]]
- [[a1b2c3]]
```

- 列表顺序即执行顺序，拖拽排序后重写该列表。
- 任务的完成状态、优先级等仍然只存在于其原始文件中；Today 文件只管"今天做哪些、什么顺序"。
- **顺延逻辑**：打开 app 时如果存在早于今天的 Today 文件且其中有未完成任务，弹出提示，一键将未完成任务转移到今天的日程（追加到末尾），历史文件保留原样（作为记录）。
- 若 Today 中引用的任务 id 已不存在（被外部删除），在 UI 中显示为灰色失效条目并允许一键清除。

### 3.5 依赖关系语义

- `A ⛔ B` 表示 A 依赖 B（B 未完成时 A 处于 **blocked** 状态）。
- blocked 是**派生状态**，不持久化：`depends_on` 中存在任意未完成任务 ⇒ blocked。
- 写入依赖时必须做**环检测**（DFS），成环则拒绝写入并在 UI 提示涉及的任务链。
- 父子关系不隐含依赖（父任务不因子任务未完成而 blocked；父任务完成状态也不自动级联，MVP 中父与子的勾选相互独立）。

---

## 4. 功能需求

### F1 — Inbox 快速添加（P0）

- 全局快捷键 `Q`（任意视图下）唤起快速添加浮层：一个单行输入框。
- 输入标题后 `Enter` 提交 → 追加到 `Inbox.md` 末尾（自动生成 id），浮层保持打开可连续录入；`Esc` 关闭。
- 输入框内支持轻量语法糖：`!1`~`!4` 结尾映射优先级（`!1`=highest … `!4`=low），提交时剥离该 token 并写入对应 emoji。
- 从唤起到完成一条录入的目标操作成本：**≤ 3 秒、零鼠标**。

### F2 — 任务树与 4 级子任务（P0）

- 文件视图 / 聚合视图中以树形展示任务，支持展开/折叠（记忆折叠状态于 localStorage）。
- 选中任务后：
  - `Tab` / `Shift+Tab`：增加 / 减少缩进层级（即改变父子关系），**第 4 级任务禁止再 `Tab`**，UI 给出轻提示。
  - `Enter`：在同级下方新建任务；`Shift+Enter`：新建子任务。
- 层级持久化为 md 缩进（4 空格/级，读取时同时兼容 tab 与 2 空格缩进，写回统一为 4 空格）。

### F3 — 优先级快速选择（P0）

- 每个任务行左侧显示优先级色条（highest 红 / high 橙 / medium 蓝 / low 灰 / none 无）。
- 两种修改方式：
  1. 选中任务后按数字键 `1`~`4` 直接设定，`0` 清除；
  2. 点击色条弹出 5 项 popover。
- 修改立即写回 md（更新/插入/删除对应 emoji token）。

### F4 — 任务依赖（P0）

- 任务详情面板（点击任务或按 `Space` 展开）中可添加/移除依赖：输入框模糊搜索全库任务（按标题），选中即建立依赖。
- blocked 任务在所有视图中：标题降透明度 + 锁形图标；hover 显示"被阻塞于：〈前置任务标题〉"，点击可跳转到前置任务。
- 前置任务被勾选完成时，依赖它的任务自动解除 blocked（派生计算，无需写盘）。
- 环检测失败时 toast 提示："无法添加：会形成循环依赖 A → B → A"。

### F5 — 聚合视图 All Tasks（P0）

- 汇总全库所有未完成任务的扁平/树形列表。
- 分组方式可切换：按文件（默认）/ 按优先级。
- 过滤器：隐藏 blocked、隐藏已完成（默认开启）、按文件路径筛选。
- 排序：组内按 优先级 desc → 文件内原始顺序。
- 每条任务显示：优先级色条、标题、所属文件名（次要文字）、blocked 标记、子任务计数。

### F6 — 今日日程 Today（P0）

- 独立视图，展示 `Today/YYYY-MM-DD.md` 引用的任务，按列表顺序渲染。
- **加入今日**：在任何视图中，任务 hover 出现"☀ 今天"按钮，或选中后按 `T`，将其 id 追加到今日列表；已在今日的任务该按钮显示为已激活态，再按一次移出。
- **拖拽排序**：Today 视图内任务可上下拖拽调整顺序（使用 dnd-kit），松手后立刻持久化新顺序。
- Today 视图中可直接勾选完成、改优先级（写回任务原文件）。
- 顶部显示进度：`已完成 n / 总数 m`。
- blocked 任务允许加入今日，但显示阻塞标记（用户可能计划"今天先做前置再做它"）。

### F7 — 文件监听与外部编辑（P0）

- 后端使用文件监听（watchdog）监视根目录，md 文件被外部（Obsidian 等）修改后 500ms debounce 内重新解析，通过 WebSocket 推送前端刷新。
- 写回策略：**读-改-写整文件 + 原子写入**（写临时文件后 rename）。写回前校验文件 mtime 与内存快照一致；不一致则重新解析后重放本次修改（按 task id 定位而非行号），仍失败则提示用户手动刷新。
- App 自身写盘引发的 watch 事件需通过写入指纹去重，避免自触发循环刷新。

### F8 — 任务编辑基础操作（P0）

- 行内编辑标题（双击或 `E`），`Enter` 保存。
- 勾选/取消勾选（点击或 `X`），勾选时写入 `✅ 今日日期`。
- 删除任务（`Del`，二次确认；连同其子任务整棵删除，并从所有其他任务的 `depends_on` 与 Today 引用中清理该 id）。

### M2（本期不实现，但架构需预留）

- 任务在文件之间移动 / 从 Inbox 归档到项目文件。
- `📅` 截止日期的完整支持与"逾期"视图。
- 已完成任务的自动归档。
- 依赖关系图可视化。

---

## 5. UI 结构

```
┌────────────┬──────────────────────────────────┐
│  侧边栏      │  主区域                            │
│            │                                  │
│  ☀ Today   │  当前视图的任务列表/树               │
│  📥 Inbox   │                                  │
│  📋 All     │                                  │
│  ── 文件 ──  │                                  │
│  projects/ │  ┌ 任务详情面板（右侧抽屉，Space 开合）│
│   ├ a.md   │  │  标题 / 优先级 / 依赖管理 / 所属文件│
│   └ b.md   │  └──────────────────────────────  │
└────────────┴──────────────────────────────────┘
```

- 四个主视图：**Today**（默认落地页）、**Inbox**、**All Tasks**、**单文件视图**（点击侧边栏文件树进入）。
- 键盘导航：`↑↓` 移动选中，`G T / G I / G A` 切换视图。
- 视觉风格：紧凑信息密度、浅色为主、无装饰性动画；优先级用色条而非大面积色块。

---

## 6. 技术方案

| 层 | 选型 | 说明 |
|---|---|---|
| 后端 | **Python 3.12 + FastAPI + uvicorn**，用 **uv** 管理环境 | 提供 REST API + WebSocket；文件监听用 `watchdog` |
| md 解析 | 自研解析器（正则 + 缩进栈），**不要**引入完整 markdown AST 库 | 只需识别 checkbox 行 + emoji token，其余行原样透传 |
| 前端 | **React 18 + TypeScript + Vite** | 状态管理用 zustand；样式用 Tailwind |
| 拖拽 | `@dnd-kit/core` + `@dnd-kit/sortable` | Today 排序 |
| 部署 | 单机本地运行：`uv run app` 同时起后端并托管前端构建产物；默认 `http://localhost:8722` | 提供 `--vault <path>` 启动参数 |

### 6.1 REST API 草案

```
GET    /api/tasks                     全库任务（树形 JSON，含派生 blocked 字段）
POST   /api/tasks                     新建任务 {title, file?, parent_id?, priority?}
                                      file 缺省 = Inbox.md
PATCH  /api/tasks/{id}                局部更新 {title? | status? | priority? | depends_on?}
DELETE /api/tasks/{id}
POST   /api/tasks/{id}/indent        {direction: "in" | "out"}
GET    /api/files                     文件树（含每文件任务计数）
GET    /api/today?date=YYYY-MM-DD     今日日程（有序任务 id + 展开的任务对象）
PUT    /api/today                     覆盖写今日 id 顺序 {date, task_ids[]}
POST   /api/today/carry-over          顺延历史未完成任务到今天
WS     /ws                            文件变更推送 {type: "vault_changed"}（前端收到后重拉数据）
```

- 所有写操作返回更新后的任务对象；错误统一 `{error: {code, message}}`，环检测失败返回 `409 CYCLE_DETECTED` 并附涉及的 id 链。

### 6.2 解析器规范（务必严格实现）

1. 任务行判定：`^(\s*)- \[( |x)\] (.*)$`。
2. 缩进 → 层级：每 4 空格一级（tab 视为 4 空格，2 空格向上取整兼容读取）；层级只能逐级加深，跳级缩进按上一任务层级 +1 处理；**解析到第 5 级时按第 4 级归并并记录 warning**。
3. 从行尾向前剥离 emoji token（顺序无关）：`✅ date`、`📅 date`、`⛔ ids`、`🆔 id`、优先级 emoji；剩余文本为 title。
4. 序列化时 token 顺序固定为：`title [priority] [🆔] [⛔] [📅] [✅]`。
5. 遇到没有 `🆔` 的既存任务：**首次被 app 修改时**才补写 id（避免 app 一启动就大规模改写用户文件）；只读展示时用 `file:line` 作为临时 key。
6. id 冲突（用户手工复制导致重复 🆔）：解析时保留首个，后续重复者视为无 id 处理并 warning。

---

## 7. 里程碑

| 阶段 | 内容 | 验收界限 |
|---|---|---|
| **M0 — 内核** | 解析器 + 序列化 + 原子写回 + 单元测试（含 roundtrip 测试：解析→序列化必须与原文件逐字节一致，仅当发生修改时允许差异） | `pytest` 全绿；对含中文、混合缩进、无 id、5 级缩进的 fixture 文件全部正确处理 |
| **M1 — 主流程** | F1 Inbox、F2 任务树、F3 优先级、F5 聚合视图、F8 基础操作、F7 文件监听 | 可完成"收集→整理→标优先级"完整闭环；Obsidian 侧同时编辑不丢数据 |
| **M2 — 执行层** | F4 依赖、F6 Today（含拖拽与顺延） | 可完成"圈选今日→排序→逐项执行"闭环 |
| **M3 — 打磨** | 全部键盘快捷键、折叠记忆、失效引用清理、空态/错误态 UI | 见第 8 节验收标准全部通过 |

## 8. 验收标准（关键场景）

1. 在 Obsidian 中手动给某任务加一行子任务并保存 → app 1 秒内自动刷新显示，且不破坏该任务已有的 id 与优先级。
2. 按 `Q` 连续录入 5 条任务（其中一条带 `!1`）→ `Inbox.md` 末尾出现 5 行合法任务，第 1 优先级者带 `🔺`，全部带唯一 🆔。
3. 建立 A ⛔ B、B ⛔ C 后尝试 C ⛔ A → 写入被拒绝，md 文件无任何变化。
4. 勾选 B 完成 → A 的 blocked 标记在不刷新页面的情况下消失。
5. Today 中拖拽 3 个任务改变顺序 → `Today/日期.md` 中 id 顺序同步改变；在 Today 中勾选完成某任务 → 其**原始文件**中该行变为 `- [x] … ✅ 日期`，Today 文件不变。
6. 昨日 Today 有 2 条未完成 → 今天首次打开弹出顺延提示，确认后 2 条追加至今日列表末尾。
7. 对第 4 级子任务按 `Tab` → 无变化并出现提示。
8. 关闭 app，直接用文本编辑器打开任意被 app 修改过的文件 → 非任务行与文件其余内容和修改前逐字节一致。

## 9. 边界情况清单（实现时必须覆盖）

- 空 vault / `Inbox.md` 不存在 / `Today/` 目录不存在 → 自动创建。
- md 文件编码非 UTF-8 → 跳过该文件并在 UI 顶部显示 warning 条。
- 任务标题中本身含有 emoji（包括与元数据相同的 emoji）→ 只从行尾按 token 规则剥离，标题中部的 emoji 不受影响。
- `depends_on` 指向不存在的 id → 忽略该依赖并 warning，不视为 blocked。
- 同一任务被同时加入 Today 两次 → 去重，保留首次位置。
- 文件在 app 打开期间被外部删除 → 其任务从视图移除，Today 中引用变为失效条目。
- Windows/NFS 场景下 rename 原子写失败 → 降级为直接写并在日志记录。

---

## 附录 A：开放决策（agent 可自行选择，需在 README 中说明）

- 前端组件库：可裸写 Tailwind，也可用 shadcn/ui。
- WebSocket 推送粒度：MVP 允许"整库刷新"信号；增量 diff 推送为可选优化。
- 快捷键方案库（如 react-hotkeys-hook）自选。